import { EntradaNumeroDirective } from './entrada-numero.directive';

describe('EntradaNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new EntradaNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
